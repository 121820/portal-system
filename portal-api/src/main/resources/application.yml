server:
  port: 8080

spring:
  application:
    name: portal-api
  datasource:
    url: jdbc:mysql://localhost:3306/portal_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: Dfsy121820!!!
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    timeout: 5000ms

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: lyx121820.portal.system.jwt.secret.key
  expiration: 86400 # 24 hours in seconds

logging:
  level:
    com.lyx.portal: debug